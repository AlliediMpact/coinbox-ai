rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all operations for testing
    // WARNING: Only use this for the emulator, never in production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
