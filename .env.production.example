# =================================================================
# PRODUCTION ENVIRONMENT CONFIGURATION
# =================================================================
# IMPORTANT: Copy this file to .env.production and fill in real values
# NEVER commit .env.production to version control
# =================================================================

# Application Configuration
NODE_ENV=production
NEXT_PUBLIC_APP_URL=https://your-domain.com
NEXT_PUBLIC_BASE_URL=https://your-domain.com
NEXT_PUBLIC_VERIFICATION_REDIRECT_URL=https://your-domain.com/auth/verify-email

# Paystack Configuration (PRODUCTION KEYS ONLY)
NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY=pk_live_YOUR_PRODUCTION_PUBLIC_KEY
PAYSTACK_SECRET_KEY=sk_live_YOUR_PRODUCTION_SECRET_KEY

# Firebase Client Configuration
NEXT_PUBLIC_FIREBASE_API_KEY=YOUR_FIREBASE_API_KEY
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=YOUR_SENDER_ID
NEXT_PUBLIC_FIREBASE_APP_ID=YOUR_APP_ID
NEXT_PUBLIC_FIREBASE_DATABASE_URL=https://your-project.firebaseio.com
NEXT_PUBLIC_FIREBASE_DYNAMIC_LINKS_DOMAIN=yourapp.page.link

# Firebase Admin SDK (Server-side ONLY - KEEP SECRET)
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk@your-project.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"
FIREBASE_DATABASE_URL=https://your-project.firebaseio.com
# Alternative: Use service account file path
# FIREBASE_PRIVATE_KEY_PATH=/path/to/serviceAccountKey.json

# Email Service Configuration (Production SMTP)
EMAIL_HOST=smtp.sendgrid.net
EMAIL_PORT=587
EMAIL_SECURE=true
EMAIL_USER=apikey
EMAIL_PASSWORD=YOUR_SENDGRID_API_KEY
EMAIL_FROM=noreply@your-domain.com

# Google Generative AI (for AI features)
GOOGLE_GENAI_API_KEY=YOUR_GEMINI_API_KEY

# Redis Configuration (for caching and rate limiting)
REDIS_URL=redis://your-redis-host:6379
REDIS_PASSWORD=YOUR_REDIS_PASSWORD
REDIS_TLS=true

# Monitoring and Logging
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
DATADOG_API_KEY=YOUR_DATADOG_API_KEY
LOG_LEVEL=info

# Security
SESSION_SECRET=GENERATE_RANDOM_256_BIT_SECRET_HERE
JWT_SECRET=GENERATE_RANDOM_256_BIT_SECRET_HERE
ENCRYPTION_KEY=GENERATE_RANDOM_256_BIT_SECRET_HERE

# Rate Limiting
RATE_LIMIT_WINDOW=60000
RATE_LIMIT_MAX_REQUESTS=100

# Backup Configuration
BACKUP_BUCKET=gs://your-backup-bucket
PDF_STORAGE_BUCKET=your-receipts-bucket

# Feature Flags
ENABLE_AI_FEATURES=true
ENABLE_PWA=true
ENABLE_NOTIFICATIONS=true

# Third-party Integrations
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL
PAGERDUTY_API_KEY=YOUR_PAGERDUTY_KEY

# Database Connection Pool
DB_MAX_POOL_SIZE=10
DB_MIN_POOL_SIZE=2

# CORS Configuration
ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com

# =================================================================
# DEPLOYMENT CHECKLIST
# =================================================================
# Before deploying to production, ensure:
# 
# 1. All placeholder values above are replaced with real credentials
# 2. Firestore security rules are deployed: firebase deploy --only firestore:rules
# 3. Firestore indexes are deployed: firebase deploy --only firestore:indexes
# 4. SSL/TLS certificates are configured and valid
# 5. DNS records point to your hosting platform
# 6. Backup cron job is configured
# 7. Monitoring and alerting are set up
# 8. Rate limiting is tested and configured
# 9. All API keys are for PRODUCTION (not test/sandbox)
# 10. Secrets are stored in secure vault (not in code)
# =================================================================
